program
{
    int c,x,z;
    write("HELLO WORLD");

    write("HELLO WORLD 2");

    if (c >= 100 or x == z)
    {
        read (y);
        write (y);
        write (x + y + z, c);
    }
    else
        c = a = 21;
    
}
